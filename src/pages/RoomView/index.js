import React from 'react';
import { StyleSheet, Text } from 'react-native';

const RoomView = () => {
  return <Text>RoomView Screen</Text>;
};

const styles = StyleSheet.create({});

export default RoomView;
