import React from 'react';
import { StyleSheet, Text } from 'react-native';

const Booked = () => {
  return <Text>Booked Screen</Text>;
};

const styles = StyleSheet.create({});

export default Booked;
