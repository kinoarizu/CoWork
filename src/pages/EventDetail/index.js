import React from 'react';
import { StyleSheet, Text } from 'react-native';

const EventDetail = () => {
  return <Text>Event Detail Screen</Text>;
};

const styles = StyleSheet.create({});

export default EventDetail;
